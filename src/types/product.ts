export interface GalleryImage {
  src: string;
  alt: string;
}

export interface AccordionEntry {
  label: string;
}

export interface RecentlyViewedItem {
  name: string;
  price: string;
  background: string;
}
